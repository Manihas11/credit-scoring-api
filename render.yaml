services:
  - type: web
    name: credit-scoring-api
    runtime: python
    buildCommand: pip install fastapi uvicorn xgboost shap pandas numpy pydantic scikit-learn
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
